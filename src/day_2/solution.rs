use crate::common;

use std::io::{Error, ErrorKind};

pub fn part_1() -> Result<i32, Error>  {
    let lines = common::read_input_for_day(1);
    for i in 0..lines.len() {
        
    }
    
    return Err(Error::new(ErrorKind::Other, "Solution not found"));
}

pub fn part_2() -> Result<i32, Error>  {
    let lines = common::read_input_for_day(1);
    for i in 0..lines.len() {
        
    }
    
    return Err(Error::new(ErrorKind::Other, "Solution not found"));
}
